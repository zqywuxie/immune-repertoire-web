# 平台优化进度（2026-09-21）

## 本轮完成

- 单项分析改为确认数据、配置分析、运行与结果三个阶段，完成后同页展示结果。
- 组合分析逐项展示真实前置依赖、历史结果来源及失败停止关系。
- 修复持久队列丢失结果登记上下文：恢复分析签名、输入资产和配置，保证项目结果登记与下载。
- Linux 部署增加维护写入锁；已有任务继续执行，有活动任务则退出部署并恢复提交。首次升级旧版本仍需人工暂停提交。
- 冷备份统一保存及恢复 .env，兼容旧 .env.docker 归档。

## 已完成验证

- 前端 111 项测试、类型检查、生产构建及桌面和移动端浏览器交互通过。
- 队列与维护模式 24 项回归通过；部署脚本 8 场景及冷备份 4 项测试通过。
- 实际差异分析到富集分析的 Redis 队列链路通过，包括维护期间已受理批次继续运行、上游输出原样复用、结果登记和压缩包下载。
- MySQL 正常停机冷备份与恢复演练通过；非 root 应用用户可读取恢复的项目、任务和结果文件。本轮未验证 MongoDB、Redis 应用级恢复及恢复后的完整分析重跑。
- 最终镜像中队列、维护模式和 Script Hub 接口共 84 项通过。接口测试注入全局 UMAP 替身，真实 UMAP 回归需独立进程运行，避免替身污染。
- API 镜像构建及完整 Python、R/Bioconductor 运行环境检查通过，复用既有依赖镜像。

## 仍需实施

- 原始 01–10 脚本逐项数值对照与 CIBERSORT 分析核验：已找到实际目录 E:/Desktop/project/immune_pipeline/pipeline；本轮完成 PEP 分组矩阵对照，其余模块仍待验证。
- 按分析模块细化样本匹配策略与完整能力矩阵。
- 服务器实际磁盘与更大数据规模的权限扫描耗时验收；本地合成目录优化已完成。
- 完整恢复环境的登录、数据库关联和分析重跑验收。

本轮仅构建并验证隔离容器，未部署到服务器。

## 后续补充：权限扫描优化

使用 scandir 返回的文件属性，减少重复查询；不跳过后续新导入文件。读取目录失败会明确终止初始化，符号链接及特殊文件不修改。

Linux 容器内 20,000 个文件、101 个目录，三轮扫描中位耗时从 0.296 秒降为 0.111 秒，约减少 63%。测试使用合成临时目录和已正确设置的权限，不能直接外推服务器磁盘耗时。5 项权限回归通过，覆盖非 root 写入、新导入文件、外部符号链接、特殊文件及扫描错误。

可在容器内运行 docker/tests/benchmark_volume_permissions.py --baseline /path/to/previous_initializer.py 复测；基准只创建并删除自己的临时目录。

模块匹配规则核对发现部分模块使用内连接，另一些模块有自定义样本编号归一化。仍需按模块分别对齐，不能统一强制完全相同的样本集合。

## 后续补充：原始脚本对齐与样本匹配

原始目录已核实为 `E:/Desktop/project/immune_pipeline/pipeline`，用户提供的旧 `immune/_pipeline/pipeline` 路径不存在。目录包含 01–10、11.BCR、公共配置和回归测试。原始目录只读挂载，不修改、不复制其业务数据。

本轮对照 `03.UCDR3/1.Pep/3.add_cate_shared.py`、`4.add_cate_usage.py` 与 `common/data/pep.py`，修复平台以下差异：

- 样本编号按文本读取，保留 001 与 1 的区别；空编号和重复编号在计算前拒绝。
- 不把缺失分组填成 0；分组输出排除缺失分组及未匹配样本，与原始脚本保持一致。
- 共享矩阵与基因使用矩阵统一解析链后缀，支持单下划线、双下划线和压缩文件名。
- 样本选择保留大小写、标点和中文，不再通过删除字符进行模糊匹配。文件名链后缀由独立解析步骤处理。

Docker 内直接执行只读挂载的两份原始脚本，以合成矩阵对照平台样本集合、分组及数值。9 项回归通过，其中 3 项直接运行原始脚本，2 项验证非法编号，4 项完整执行易混淆编号的单样本选择。对照保留平台既有 Category/类别行命名契约，按列名比较数值，不声明图形排序完全一致。前导零输入的真实 PEP→UMAP→下载链路通过。

复测原始对照需挂载参考目录并设置 `REFERENCE_PIPELINE=/reference`，运行 `flask_app/tests/test_pep_reference.py`；没有参考目录时仅跳过 3 项原始脚本对照，其他测试照常执行。

未完成：跨批次同名样本的复合标识接入、其余 01–10 模块数值核验、免疫浸润结果及通路关联接入。当前重复样本明确拒绝，尚不能宣称支持原始脚本的完整批次选择能力。

## 后续补充：免疫浸润输入校验

对照原始 07.immuneInfiltration 的组成图脚本及公共帮助函数，平台现在分别记录细胞列、分组列和质量指标列。Group、category、分组按文字保留；P-value、Correlation、RMSE 等不计入细胞列，仍检查其数值内容。只包含质量指标而没有细胞列的表格拒绝通过。未知文字列不会被静默丢弃；本次不推断相对或绝对模式，不归一化数据，不改变负值。

校验器版本从 2 升为 3，已上传资产的旧校验摘要自动失效并按新规则重查。现有输入质量面板通过中文提示展示分组与质量指标识别结果。

Docker 中输入校验和上传缓存共 26 项测试通过，覆盖 CSV、工作表、前导零、非法数值、旧版缓存以及直接运行原始 R 帮助函数的细胞列对照。此对照只验证输入列识别，不是浸润统计量或图形数值核验。

组成图及组间比较现已接入，见下一节。仍待实现：一致性和通路关联的任务与结果接入；相对与绝对结果应分别选择，不能仅凭数值范围推断。部署到服务器后需重新检查此前选择的浸润输入。

## 后续补充：免疫浸润组成与组间比较

- 分析中心新增“免疫浸润组成与比较”，读取当前项目、当前数据集的样本指标表及免疫浸润表，支持确认分组和细胞范围后执行。
- 浸润样本必须全部匹配分组；至少两个分组且每组至少两个样本。未使用的指标表样本显示数量；保留编号前导零。更换分组或细胞范围后必须重新核对。
- 使用原始 07 目录两份 R 分析脚本，保留全局最小值平移加微量值后按行归一化、双侧 Wilcoxon 及所有细胞/组对的全局 BH 校正；箱线图星号仍基于原始 p 值。原始分数及校正统计表均可下载。
- 支持单任务及组合任务的真实 Redis 队列、取消检查、任务阶段进度、项目结果登记、图表预览和压缩包下载。结果页不对整张组成图标记显著性。
- Docker 中真实 R 接口回归 2 项及隔离 Redis 批次回归 1 项通过，验证数值、样本编号、SQL 资产登记与下载；Mongo 保存使用测试替身，未宣称完成 Mongo 集成验收。原有 Script Hub/持久队列 81 项通过。前端原有 111 项与新增配置交互测试通过，生产构建通过。
- 登录后的生产页面浏览器链路通过，包含数据检查、配置确认、实际计算及结果展示；390 像素移动端未发现横向溢出。构建 API 和网页镜像并通过完整运行环境检查。部署仍需服务器执行既有部署流程，本轮未操作服务器。

后续优先级：免疫浸润相对/绝对输入分别选择与一致性分析；通路关联及上游输出接入；其余 01–10 脚本数值核验；跨批次样本复合标识。服务器磁盘实测及完整恢复验收仍未完成。
