# 完整计划执行记录

更新：2026-09-13。目标：执行 project-input-workflow-plan.md 全部要求。状态：执行中，未完成。

## 第一阶段已落地

- 内部容器模式关闭登录和注册流程，网页直接进入工作台，账号及退出入口隐藏。
- 内部请求忽略既有登录会话，不修改项目与用户的原归属；内部文件浏览限制在配置数据根目录。生产/原容器认证模式仍保持登录要求。
- 增加 deconvolution/cibersort 资产识别、上传、数据集收集、向导选择和检查响应；兼容 datapoint/profile。
- 克隆序列表增加多文件直接上传，服务器目录导入折叠为备用操作。
- 项目与数据集标识保存于会话，刷新后重新读取真实资产，不持久化失效文件路径。
- 项目数据管理将第四类资产纳入分析数据，而非附件。

## 当前验证证据

- 隔离 Linux 容器：内部认证/生产认证/公共边界 18 项通过。
- 隔离 Linux 容器：指标分析与四类输入数据集隔离 3 项通过，包含实际指标图表和表格生成。
- 前端 22 个测试文件、75 项通过，最终生产构建通过。
- 浏览器容器：实际免登录用户接口和项目列表返回成功；登录/注册地址跳转正确，账号操作隐藏。
- 浏览器合成项目：四类数据卡显示、数据集刷新恢复、390px 布局通过，无运行时错误。此项未提交科研任务。
- 已更新本地 api/worker/web，六个服务健康；更新前确认工作进程空闲、排队数为零。
- 当前 API 镜像：4334955a0ea4be90d9b59d0647a03a5847d0308a771dc701b8045187679923a1。
- 当前网页镜像：2a70df0666101807b4fc1114130c8c3002f181a94a6729e827b7d8ed8d0a2f01。
- 回退标签：immune-platform-api:before-input-foundation、immune-platform-web:before-input-foundation；配置回退时恢复 FLASK_CONFIG=container。

## 必须继续完成

1. 输入检查：四类内容检查、样本列/工作表/批次/分组映射、样本不匹配提示与预览。当前第四类仅文件可读性与资产贯通，不能认为浸润分析已实现。
2. 数据准备：原始与派生样本表分离、脚本准备步骤及映射登记；数据卡布局、中文名称和检查状态完善。
3. 分析依赖：按项目/数据集/模式匹配产物、后端产物标识解析与执行时复核、记录来源、上游跳转与下游继续分析、旧结果兼容。
4. 分析中心：真实输入与前置产物就绪状态；上下文固定；避免每次重复上传和检查。
5. 进度：真实阶段、连接恢复、全部终态、取消中状态、批次部分完成、日志去重折叠、刷新恢复；组合任务不能依赖前端页面继续提交。
6. 结果：图表与表格页内预览、筛选分页、批量归档下载、缺失/空结果区分、历史与新结果共用展示、来源追溯。
7. 转录组差异结果→富集复用；免疫浸润实际脚本面板接入，按面板检查所需表达/通路等依赖；参考脚本数值与输入契约回归。
8. Linux：跨平台初始化、明确完整镜像版本与架构、必要脚本/清单打包、挂载与权限、资源/并发/模块超时、日志轮转、工作进程自身健康与主机重启对账、大文件、备份恢复和干净部署。
9. 科学依赖/参考资料版本记录、容量与临时数据生命周期；不删除被引用结果。
10. 完整镜像构建、合成分析上下游真实回归、浏览器闭环和最终部署验收。目标服务器尚未提供，当前本机 Linux 容器验收不能替代实际目标服务器验收。

每项以实际源码及验证结果更新。不得把当前基础输入阶段标记为完整计划完成。

## 第二阶段进行中：结果引用与 Linux 初始化

- 新增 `docker/init_env.py`，在容器内生成独立密钥和完整分析镜像配置。独占创建、不覆盖已有环境文件；Linux 新建文件权限为 600。Windows 入口调用同一容器脚本，Linux 命令使用当前用户写入。
- Compose 默认镜像与初始化器统一为 full，显式 core 配置保留。部署说明补充 Linux 启动方式。本次未更改运行中环境文件、业务数据卷或重启服务。
- 上游结果服务及界面改动通过本轮回归：项目/数据集范围、源任务状态、文件存在、来源输入变化、执行前复核；前端恢复产物标识选中项，并验证唯一候选预选、多个候选手选及禁选缺失结果。
- 容器验证：初始化 2 项通过；分析产物、Profile 与 Script Hub API 合计 68 项通过；现有前端 75 项通过，新增结果选择交互 3 项通过；最终生产构建通过。Compose config --quiet 与相关 diff --check 通过。
- 此阶段结果引用尚未构建发布到当前应用，仍需真实 PEP 上游到下游执行闭环、候选类型/条件对照、结果页继续分析入口及浏览器验收。初始化测试不能替代整套空服务器部署验收。
- 完整计划保持执行中。下一步优先完成结果复用闭环，再推进四类内容检查、进度结果交互、浸润分析及剩余 Linux 部署事项。

## 第二阶段补充：结果到下游的界面入口

- 新建共享 ContinueAnalysis 组件，新结果页与历史 JobResultPanel 均接入。仅完成的 PEP 来源任务、可确认项目/数据集和当前任务的可用产物提供继续分析入口。
- V/J 产物可进入使用差异、特征降维和机器学习；汇总特征表进入特征降维/机器学习；TRA 共享产物进入 MAIT/NKT。链接只传项目、数据集和产物标识，下游提交仍由后端解析路径。
- 下游向导初始化读取产物标识，机器学习选择 vj 模式，MAIT/NKT 选择 pep_analysis 来源；数据集变更或新建分析清除旧引用。同项目不同数据集打开链接不复用会话内其他数据集的文件路径。
- 已核实 UmapinService 支持使用频率目录和汇总 CSV，因此保留两类输入候选，并未仅凭候选名字删除目录兼容。
- Docker：前端全量 24 文件/81 项通过；随后新增上下游向导传参回归，相关 2 文件/10 项通过；最终 TypeScript 和生产构建通过。当前前端测试共 82 项，最终新增项之后未重复跑全量。相关 diff --check 通过。
- 此项为界面与请求契约验收，未部署到现有服务。下一步仍需真实小型 PEP 产物生成、完整来源登记、下游执行与下载、容器浏览器及最终镜像发布；不能以当前模拟候选测试替代科研闭环。完整计划其余输入内容、进度结果、浸润及 Linux 部署事项继续保留。

## 第二阶段补充：真实 PEP → 特征降维回归

- 新增 test_pep_downstream_workflow.py：8 个合成样本、4 个 V/J 特征，真实执行 PEP 必需步骤、生成共享矩阵和带分组汇总表，读取真实缓存清单并通过 SQL 来源任务归属解析产物。仅旧 Mongo 空记录入口隔离，不模拟候选列表或计算。
- 验证来源数据集隔离、任务标识映射、执行前复核、实际检查接口、copy 加权频率每行和为 1、UMAP 坐标有限且保留 8 个样本和两组、图表生成及实际结果下载字节一致。
- 发现并修正候选语义：UMAP 候选只在所选产物目录内核对分组列，不借用旁边汇总表来宣称当前原始目录可用；无分组列显示明确原因。继续分析入口按后端提供的 umapin 能力展示。
- PEP 当前汇总表仅采用第一个分组字段，候选元数据相应仅登记该字段，避免宣称能复用全部分组。
- Docker 最终后端 6 项通过（含真实计算）；前端候选/继续分析 6 项通过，生产构建通过。真实计算目前调用服务并登记合成来源任务，未覆盖 HTTP 提交经 RQ 执行全链，不能替代排队工作进程验收。
- 尚未发布本阶段源码。继续进行其他下游模式与分组条件检查、浏览器验收、镜像更新，以及完整计划中的输入内容、进度结果、浸润与 Linux 部署等剩余要求。

## 进度展示阶段：批次终态与日志

- 新增 BatchStatus 共用状态展示。结果页按计划模块数及任务数计算总数，不因首个结果返回成功就将整批标记完成；分别展示失败、取消、中断和部分完成。
- Stage4 两类任务轮询均将 interrupted 作为终态，最终日志使用中文状态并报告成功数量，不将结束一律描述为成功。
- 执行日志默认折叠、按任务状态变化去重，界面最多保留最近 200 条；折叠时不自动滚动页面。日志使用现有浅色界面变量。
- 修复 Stage4 原路径校验对产物标识请求的阻拦，UMAP、V/J 差异和 PEP 来源 MAIT/NKT 可仅携带已选产物标识，后端仍负责解析与验证。
- Docker：新增 4 项批次/执行回归通过，Profile 原上传/执行 4 项通过；前端全量 25 文件/86 项通过，生产构建通过。相关 diff --check 通过。
- 未部署本阶段变更。组合分析仍由浏览器逐模块提交，尚未完成服务端批次推进、连接恢复、取消中状态、真实阶段细化及持久批次恢复；这些仍是完整计划必须完成项。结果页内预览/批量归档、四类内容检查/准备、浸润闭环、Linux 运行资源及备份恢复等同样未完成，不以本次进度显示修复代替。

## 服务端批次执行：核心与页面接入进行中

- 新增 analysis_batch_service.py 和 POST /api/script-hub/batches。项目归属、数据集、允许模块及计划结构检查后，完整计划存入 AnalysisJob，再提交现有 Redis/RQ 队列。
- 工作进程读取持久计划，复用实际 API 校验与模块执行入口；用 ContextVar 限定批次上下文，ScriptExecutor 和 RedisJobQueue 在本工作进程中执行子任务，先登记父子关系，避免单工作进程等待自身队列。各项结果更新到父任务 items；独立项失败保留原因，后续独立项继续；取消后剩余计划项标为取消。
- Stage4 多模块改为一次提交完整批次，单模块契约保持；保存 batch/job 地址，按父任务快照收集子任务结果。查询连接中断后重试读取，不重新提交批次。
- 同步 ScriptHub 和 OpenAPI/生成 Job 类型中的 interrupted 状态；前一轮构建在新增测试前通过，本轮加入测试后发现类型遗漏并修复，最终构建通过。
- Docker：服务端 4 项隔离回归通过（派发器与入队模拟，真实数据库计划/状态/父子关联）；前端批次与向导 12 项通过；最终 TypeScript/生产构建与相关 diff --check 通过。
- 本阶段未部署，服务端批次尚未完成真实 RQ 单工作进程计算验收。必须继续：当前子任务取消传递、无子任务编号的提交失败/取消项在结果页准确展示、批次历史展开和刷新恢复、明确依赖节点失败处理、父任务重试契约、意外退出状态同步。现有前端结果统计仍需接入完整 batch items，不能只统计有子任务编号的结果。
- 完整计划保持执行中；此前未完成的四类内容检查/准备、结果页内预览和归档、浸润接入、Linux 资源和恢复、浏览器及最终镜像发布仍须完成。

## 批次历史、恢复与取消传播补充

- 新增 BatchResultPanel，共用 JobResultPanel 在历史中展开完整批次计划，默认按需读取首个成功子结果，支持切换其他子结果；失败/取消项目即使没有编号也显示原因和状态。
- useJobResult 对批次始终读取任务快照，包括运行中、部分失败、中断和取消，不因父批次失败而隐藏成功产物。连接错误保留最近快照和实际错误原因，3 秒后自动重读；终态停止自动轮询。
- Stage4 回传完整批次 items 状态，Stage5 按完整计划统计，全部项目在提交阶段失败时也显示已结束，不无限等待子结果。
- 运行中批次取消先保留 running 并显示“正在取消”，通过数据库父子关系传递到 JobContext 和 Script Hub 计算检查点；当前任务返回后再结束批次。排队批次取消立即更新计划各项。此阶段仅批次采用延迟终态，独立任务取消模式仍需统一。
- Docker：队列及批次后端 27 项通过；前端全量首次 89 通过/1 失败，失败是重连提示丢失原错误，已恢复具体原因，相关历史/批次/统计 13 项通过；最终生产构建与相关 diff --check 通过。没有在最终文案修复后重复全量测试。
- 未部署本阶段源码。服务端批次真实 RQ 单工作进程、实际取消/重启、父批次重试、依赖节点失败阻断及浏览器验收仍需完成；不将组件模拟和服务层状态回归当作真实工作进程闭环。完整计划其他输入内容、分析脚本、结果预览归档、Linux 运维和最终发布事项继续执行。


## 批次真实执行及 Linux 工作进程健康检查

- 批次计划支持依赖前序索引；上游失败阻断依赖项，其他独立项继续。父批次重试创建新计划并保留原记录。前端自动建立分析依赖和绑定本批次上游产物尚未完成。
- 真实 Redis/RQ 单工作进程执行两个合成 Profile 分析，验证父子关系、完成状态和 CSV 实际下载。该测试本轮最终重新执行，1 项通过。不是 PEP→下游的 RQ 全链验收。
- 真实执行暴露并修复缓存来源代码缺少 Flask request 导入、通用结果清单漏收 csv_urls/pvalue_urls。取消传播引起非 Redis 进程内旧任务访问不存在数据库表的问题已修复；Redis 模式仍抛出数据库错误。批次、持久队列和 Script Hub API 回归最终 84 项通过。
- worker_main 使用当前容器主机名建立确定的 RQ 工作进程名称；新增 healthcheck 检查本进程注册、监听队列、状态、心跳和 Linux 进程存活。替代任意 Worker.count 大于零的健康检查。
- 独立 Redis 测试队列和真实 Linux 子进程验证其他工作进程不能掩盖当前进程缺失、健康心跳、过期心跳与进程停止，2 项通过。未操作业务任务与数据。
- Compose 全部常驻服务显式使用 local 日志驱动，轮转上限 20m、保留 5 份。Compose config --quiet 及相关 diff --check 通过。
- 尚未构建发布这些源码与配置；实际服务仍需统一镜像更新后验证。非 root 和卷权限、资源限额、实际取消/主机重启对账、备份恢复、大文件与干净 Linux 部署未完成。
- 完整计划继续：四类内容与样本检查、真实上下游自动绑定、结果页内预览归档、浸润和脚本对照、容器浏览器验收及最终发布。不得以本轮测试代替完整计划完成。


## 共享结果页内预览

- 新结果 Stage5 与历史/批次子结果共用 JobResultPanel；按所选文件加载 ViewArea，提供图像、报告和数据表页内预览，移除重复来源信息。
- TablePreview 流式读取最多 2 MB，保留至多 1000 条完整记录；25 行分页和预览范围内筛选。CSV 引号、逗号、换行、BOM、制表符处理保留原始字符串，不自动转换编号或科学计数法。大表明确说明预览范围，保留完整下载。完整数据服务端分页尚未实现。
- 区分空文件、仅表头、筛选无记录、文件缺失及读取失败；重试和切换文件时清除旧内容。图像加载失败提供重试。电子表格文件不再误识别为 CSV；当前仍下载，不宣称支持工作簿页内预览。
- 修正结果选择框可访问名称和预览输入框样式。TSV 的资产接口即使无扩展名也按输出类型选择分隔符。
- Docker 前端全量 94 项通过；最终格式支持后预览 4 项通过，最终 TypeScript/生产构建通过。相关 diff --check 通过。
- Docker Chromium 在临时前端中使用合成 API，实际 /analysis/tools/profile?job=...&project=... 路由验证分页、筛选、图片切换、390px 无横向溢出和无运行错误。截图保存于本轮可视化目录 result-table-desktop.png / result-table-mobile.png。临时 Vite 在 Windows 绑定目录修改后需重启才能加载新源码；最终浏览器通过。
- 尚未发布到当前服务。批量单包归档、完整数据服务端分页、四类输入与样本检查、分析依赖自动绑定、浸润及脚本验证、剩余 Linux 运维与最终部署仍在完整计划内。


## 所选结果单包下载

- 新增 POST /api/jobs/results/archive，选择项包含 job_id 与结果 URL。后端验证任务访问权和结果清单成员关系，再通过既有本地文件路由读取；拒绝外部地址，不接受文件系统路径。支持 Script Hub、资产以及已确认的图表结果路由。
- 所选文件按任务分目录，重复选择去重，同名文件带序号，资产接口使用响应中的中文原文件名和扩展名。超过 8 MB 的压缩输出使用临时文件；响应关闭或失败时清理临时文件。默认读取总量限制 2 GiB，可通过 RESULT_ARCHIVE_MAX_BYTES 配置。
- 任一文件缺失或不可读取时返回错误，不交付缺文件的成功包。前端 Stage5 改为一次请求下载压缩包，失败保留选择并允许重试。
- Docker 归档后端最终 5 项通过：跨任务内容、去重、权限、缺失、限额、中文原文件名；前端相关 9 项通过，最终生产构建通过。
- 扩展真实 RQ 合成分析回归：两个 Profile 子任务完成后，通过实际结果与文件接口打包两个 CSV 并解压核对。1 项通过。不是所有模块组合的归档验收。
- 尚未部署。浏览器端目前用 Blob 接收包，大文件下载仍需持久归档任务或可直接下载的地址，避免浏览器内存压力；历史页所选文件批量下载尚未接入，已有完整结果包下载保留。API 文档同步、归档下载浏览器验收及完整数据服务端分页仍待完成。
- 四类输入/样本一致性、自动分析依赖、浸润脚本、Linux 权限资源和恢复、最终镜像部署仍属于完整目标，未完成。


## 完整结果数据表分页与筛选

- 新增 POST /api/jobs/{job_id}/table-preview，复用结果归属与任务访问权检查，通过现有本地文件接口逐行读取 UTF-8 CSV/TSV。筛选全表、返回当前页及原始行数/匹配行数；不将整表载入数组，不自动转型样本编号和科学数值。每页 1–100 行，页面默认 25 行。
- 新结果、历史结果和批次子结果经 JobResultPanel/ViewArea 接入 ServerTablePreview。点击筛选后请求完整表；切换文件重置条件，取消旧前端请求；空表、失败、格式错误和无匹配分别显示。无任务上下文的旧独立预览保留明确标注范围的本地预览。
- API 文档补齐归档和数据页接口；使用 Docker 中 openapi-typescript 重新生成 schema.ts。构建发现新增测试使用了不支持的 exact 属性，已修复，最终生产构建通过。
- Docker 后端分页及归档 12 项通过，包括第 1175 行起分页、检索第 1100 行、保留前导零/科学计数法/引号换行、TSV、空文件、错误格式、越权、缺失与非法参数。前端预览 6 项通过，全量 29 文件/97 项通过。
- 实际 RQ Profile 双任务回归加入真实 CSV 分页读取与字符串类型检查，计算→结果接口→分页→跨任务压缩包完整回归 1 项通过。
- Docker Chromium 在实际结果路由验证分页、1200 行合成表中筛选第 1100 行、切换图片、390px 无横向溢出及无运行错误。浏览器使用合成 API，不代替实际服务器验收。临时前端验收后关闭。
- 该实现每次查询按流扫描源表，超大表的索引/查询耗时仍需按实际数据规模评估；尚未支持工作簿分页。源码未发布到当前服务。
- 完整目标继续：大文件归档直接下载、历史选择下载、四类输入/样本校验、自动上下游依赖、浸润与脚本对照、Linux 权限资源/备份恢复及最终镜像部署。


## 四类输入的样本质量检查

- 新增 input_quality 服务并接入 data-selection/inspect。项目请求仍仅读取服务器所选数据集的登记资产；客户端传入其他路径不会替代已登记资产。
- 样本指标表和浸润表检查已识别样本编号列的空值、重复值及其他字段空值；保留字符串前导零。PEP 按既有文件名/链规则列出样本并报告重复样本链组合。表达矩阵检查首列基因标识及样本列名，读取原始首行避免 pandas 重复表头重命名掩盖重复样本。
- 以样本指标表优先的样本集合为参照，返回每类输入一致/缺少/多出样本数量及前 50 个差异编号。无法识别的列或方向报告待确认，不自动改名、删除样本或构建映射。
- 新增 InputQualityPanel，检查步骤展示四类输入状态、字段空值和样本差异明细；存在空编号或重复编号错误时向导阻止进入配置，并提示修正后重新检查。这里只是检查和界面门禁，尚未给所有提交入口/工作进程接入同一复核。
- 原有 profile-only 样本摘要读取改为字符串，避免与新检查报告的前导零编号不一致。
- Docker 输入质量/Profile 回归 8 项通过；最终输入质量/Script Hub API 回归 65 项通过；相关前端 7 项通过，最终 TypeScript/生产构建和相关 diff --check 通过。本轮未做新面板浏览器验收或发布。
- 尚未完成：工作簿工作表选择、自定义样本/分组/配对映射、表达量与浸润数值内容核验、映射实际进入分析流程和提交时复核。表达矩阵目前仅核对表头样本，不宣称已验证全部数值。
- 完整计划其余大文件下载、历史归档、自动依赖、浸润/脚本对照、Linux 权限资源/恢复和最终部署继续保留。


## Linux 普通用户运行与数据卷权限

- 精简与完整 Dockerfile 创建 UID/GID 10001 的 immune 用户，设置家目录并以该用户运行应用；新数据目录在镜像中拥有正确所有权。完整运行环境检查移至该用户下执行。
- 新增仅 operations 配置启用的 volume-init 服务及 init_volume_permissions.py。只处理应用数据和临时卷，补齐所有者读写权限，拒绝链接根目录并跳过符号链接；不触及数据库服务卷。迁移步骤、已有卷升级和测试 tmpfs 的 UID/GID 配置已写入部署文档。
- Docker 隔离权限测试 2 项通过：内容不变、外部链接目标不改属主、UID 10001 能追加原文件并生成新结果、链接根目录被拒绝。
- 使用现有完整镜像加最新源码只读挂载，以 --user 10001:10001 运行实际 RQ 双 Profile 分析，结果分页和压缩包下载回归 1 项通过。以相同用户执行现有镜像 check_runtime.py --full 通过，含 SoNNia 与 R/Bioconductor。这些是用户覆盖验证，不是新镜像构建完成证据。
- Compose config --quiet 及相关 diff --check 通过。当前业务卷未迁移，运行中服务未重启。
- 已启动实际完整 Dockerfile.analysis 构建，目标 immune-platform-api:nonroot；本记录时仍在安装 Python 依赖，未完成最终科学依赖/用户/启动验收。须继续跟踪同一构建，终态后再检查新镜像默认用户、新卷权限与真实分析，不能按已有镜像测试直接宣称完成部署。
- 完整计划其他输入映射/提交复核、自动依赖、浸润脚本、大文件与历史下载、Linux 资源/恢复及最终发布继续执行。


## Linux 资源限制与 Docker 启动文档

- Compose 新增接口容器默认 2 核/2g、分析容器默认 4 核/8g 配额，进程上限 512。CPU/内存、任务超时和工作进程停止宽限可通过环境配置调整；数值库默认 1 线程，减少多个任务内部并行争用。这些是初始可调上限，不是最低硬件配置或所有模块负载测量结论。
- 初始化器为新配置写入资源变量，已有配置不改写。2 项初始化回归通过；Compose config --quiet 及相关 diff --check 通过。
- 隔离容器以 UID/GID 10001 运行，实际核对 cgroup v2 memory.max=8 GiB、CPU 配额=4、pids.max=512，然后执行真实 RQ 双 Profile、结果分页和压缩包下载回归，1 项通过。现有服务尚未重新创建，不宣称已应用资源限制。
- dev-startup-guide.md 已移除旧宿主机安装/运行示例，统一为 compose.docker.yml、容器测试、普通用户数据卷升级及状态检查；不改变原有用户数据。
- 完整镜像构建仍为同一个任务，Python 依赖已安装，当前进入 R/Bioconductor 包下载和安装。途中 formatR 二进制包出现一次 SSL 下载错误，安装器仍继续；必须等待终态再判断并修复，不能将当前阶段当作成功。目标镜像 immune-platform-api:nonroot 尚未验收或部署。
- 后续继续新镜像默认用户和新卷验证、Linux 备份恢复/重启对账、输入映射和提交复核、上下游自动绑定、浸润脚本、历史/大文件下载及最终发布。


## 冷备份工具验证与完整镜像构建修复

- Docker 中 cold_backup 隔离测试 3 项通过，覆盖五卷文件内容、UID、内部链接、配置权限、拒绝覆盖与非法路径；未操作现有业务卷。补充 Linux 停机备份及独立空卷恢复命令，完整数据库启动恢复验收仍未完成。
- 上次完整镜像构建已终态失败：R 包下载出现 SSL 错误，最终 DOSE 安装非零和依赖加载检查失败。新增 install_analysis.R，最多重试三次并保持最终全部必需包加载门禁，不接受依赖缺失镜像。
- 修复后启动新构建，已有系统和 Python 层命中缓存；新镜像仍未完成验收或部署。完整计划保持进行中。


## 三类数据库真实冷恢复演练

- 独立 Docker 容器和十个源/目标数据卷、一个临时状态卷运行合成数据演练：MySQL、MongoDB、Redis 写入后正常停库，备份五卷和配置，恢复到空卷，然后重新启动数据库读取原记录。
- 首轮实际发现 mysql_data/mysql.sock 绝对路径链接被恢复过滤器拒绝。备份仅排除该位置的符号链接，其他链接继续受路径校验；补充对应回归后冷备份单元测试 3 项通过。
- 修复后真实演练退出码 0：MySQL、MongoDB、Redis 均读回 sample-001，应用 CSV、临时文件和配置内容一致。编排脚本 finally 清理本次创建的容器和卷，不涉及业务数据。脚本保存为 docker/tests/test-cold-restore.ps1，默认仓库路径从脚本位置推导。
- 仍需恢复环境的平台项目/资产和真实分析闭环验收；完整新镜像构建仍在执行，未部署。完整计划其他输入映射、提交复核、上下游绑定、浸润脚本及大文件结果等事项保持未完成。


## 转录组输入数据集隔离修复

- 发现 _transcriptome_path_from_request 忽略 asset_set，并在项目缺少输入时回退到客户端路径。改用共用项目资产解析，始终限定所选数据集；缺少输入返回空值，交由现有分析参数校验提示，不混用其他数据集或客户端文件。无项目的原独立工具契约保留。
- Docker 输入质量回归 6 项通过，含两个真实登记数据集、错误客户端路径、缺少数据集和独立工具路径；Script Hub API 60 项通过。相关 diff --check 通过。尚未部署。
- R 重试构建已终态失败，确认已安装二进制包仍缺 GenomeInfoDbData 间接依赖。安装器新增完整 Depends/Imports/LinkingTo 依赖集合，重试补齐缺失包。已启动修复后新构建，未完成镜像验收。
- 服务端全入口输入质量复核及工作进程复核仍未完成；本轮先修复使检查/运行可能选择不同数据集的确定性错误。完整计划继续。


## Script Hub 提交和工作进程输入复核

- 新增 validate_analysis_inputs，按实际 input_assets 复核原始 profile/datapoint、transcriptome/expression、deconvolution/cibersort 表的当前内容；读取失败、空样本编号、重复样本编号抛出带质量详情的中文 ValidationError。派生 cached_usage 不按原始矩阵误判；未知列映射和跨表一致性仍保留待完善。
- 共用缓存上下文构建在查缓存或入队前复核；无项目任务也保留输入描述，供工作进程使用。持久 execute_script 在上游引用校验后、计算函数前再次复核，覆盖经过此入口的独立 Script Hub 和批次子任务。
- Docker 输入质量与 Script Hub API 68 项通过；持久队列与真实 RQ 双 Profile→结果分页→压缩包回归 13 项通过。新增工作进程测试确认入队后输入变成重复编号时不调用计算函数。相关 diff --check 通过。
- 范围尚不等于全部入口完成：通用任务执行入口、无输入描述的旧任务、PEP 内容核验、表达/浸润数值核验、工作表与样本映射仍需继续；不改变统计定义。源码未部署。
- 完整镜像仍为 session 73429，继续安装依赖；构建上下文在本轮输入复核修改前捕获，新镜像成功后还需包含最终源码重建。完整计划其他依赖自动绑定、分析脚本对照、结果和 Linux 验收继续。


## 原始矩阵数值检查与新镜像验收

- 输入质量新增表达矩阵/浸润表数值扫描：空值、非数值、NaN/Inf 和空数值区域报错，返回总数与前五个行列位置。CSV/TSV 按行读取，xlsx/xlsm 只读扫描首张工作表；旧 xls 使用既有读取方式。表达矩阵不整体载入内存，不自动填补或改变单位，不将所有负值判错。
- 同一检查自动作用于先前已接入的提交与 Script Hub 持久执行复核。Docker 输入及 API 回归 70 项通过，含第 2502 行错误、科学计数法、负值和浸润表错误内容。
- 完整 R 依赖构建成功，GenomeInfoDbData 等间接依赖已补齐；普通用户下 check_runtime.py --full 通过。随后纳入最新业务代码的镜像成功构建：immune-platform-api:nonroot，sha256:b1485f0c81581fa3fedfb23b9d0e15b56cb356c2ff20b29087a09b8311a2b599，默认用户 10001:10001。
- 使用该镜像默认用户、新匿名卷、镜像内业务源码运行真实 RQ 分析与输入测试。首轮 9 通过/2 失败，失败来自测试 Flask root_path 指向只读源码目录；夹具改用 pytest 临时目录。仅挂载修正夹具后，RQ、输入质量与 Profile 回归 14 项全部通过。此夹具修改尚未重建入镜像，业务源码已包含。
- 当前生产服务和旧业务卷未更换。新卷普通用户分析验证完成，旧卷权限升级和统一服务发布、平台恢复验收仍待进行。工作表选择、列映射、通用入口、上下游自动绑定、脚本对照和大结果下载等完整计划事项继续。


## 批次前序产物绑定

- 批次项增加 upstream_from 前序索引，校验来源必须为前面的 pep-analysis，目标模式必须支持既有产物引用，并自动加入 depends_on。
- 派发时在任务所属用户上下文查询当前项目/数据集的产物，只匹配该前序子任务 job_id、可用状态及链/分组条件。唯一匹配才覆盖旧路径和旧引用；无结果或歧义明确失败，后续接口继续走既有产物解析和工作进程复核。
- Docker 普通用户下批次服务与真实 RQ Profile 回归 15 项通过，新增测试确认旧项目结果不能替代本批次产物。相关 diff --check 通过。此测试尚非实际 PEP→下游 RQ 端到端绑定验收。
- 前端批次参数类型、依赖选择及自动组装未接入；API 文档同步、实际 PEP 链验证、DEG→富集依赖仍需继续。新代码尚未构建发布，完整计划保持进行中。


## 批次结果来源交互

- Stage4 新增符合模式的下游结果来源选择：使用配置中的已有结果，或本批次前序克隆共享分析结果。显示中文依赖及失败阻断提示，执行中禁用修改。
- 选择批次结果后只跳过尚未生成的缓存路径校验，其他分组/样本参数仍校验。API 提交保留 upstream_from/depends_on，服务端继续负责精确绑定。不会发送虚构产物编号。
- Docker 前端生产构建通过；批次进度与历史交互 9 项通过，新增用例验证无缓存路径时提交明确前序索引。
- 当前仅支持选择顺序中已有前置 pep-analysis，Stage3 配置表单对尚未生成产物的提示/门禁和分析顺序调整仍需完善；尚未进行浏览器与真实 PEP→下游整链验收，未部署。完整计划继续。


## 依赖驱动的批次顺序

- 运行步骤允许在任意勾选顺序下选择使用本批次 PEP 结果。仅当明确选择新结果且源模块排在下游之后时，将源移到首个依赖项之前；保持其他模块相对顺序。
- 页面显示最终执行顺序，提交索引按最终计划计算；已有结果模式不产生依赖或自动排序。
- Docker 批次交互 7 项通过，覆盖前序先选/后选均提交 pep-analysis→umapin 和依赖索引 0。前端生产构建及相关 diff --check 通过。
- 尚未发布。配置阶段提示、浏览器验收、实际 PEP→下游 RQ 链与完整计划其余事项继续。


## 真实 RQ PEP→特征降维→结果下载闭环

- 新增 test_pep_batch_rq.py：8 个合成样本，真实 PEP 共享计算后，本批次 umapin 使用 upstream_from 绑定刚生成的产物，执行真实降维，验证父子任务完成、来源任务引用、两个任务 CSV 下载/分页和跨任务压缩包内容。
- 实际运行暴露并修复三类问题：可选步骤 set 无法保存 JSON（队列编码/解码保留集合）；PEP 缓存误用报告编号作来源任务编号；结果登记项目根目录与项目资产读取不一致导致跳过登记。
- PEP 同一汇总内容同时输出 df_VJ_all.csv 和 df_1VJusage_all.csv，并暴露父目录候选。批次 umapin 明确绑定规范 df_VJ_all.csv；缺少特征范围时从生成后的分组列之后读取范围，不伪造路径或预先编号。
- 计算完成后发现统一结果接口遗漏 PEP CSV，补齐共享矩阵、使用频率、热图数据、分类和比例数据列表，使现有页内预览/下载可用。
- 最终 Docker 默认普通用户下真实链路+归档/表格测试 13 项通过（49.56 秒）；此前集合修复及批次/持久队列 27 项通过，相关 diff --check 通过。未操作生产数据。
- 此证据覆盖 PEP→umapin 一条真实链，不代表火山图/机器学习/MAIT 或 DEG→富集链均完成。完整计划剩余映射、配置提示、浏览器验收、其他分析与最终镜像发布仍继续。最新修复尚未构建发布。


## 工作表与列映射的结构查询

- 新增项目限定 POST /api/script-hub/data-selection/table-schema，使用项目所选数据集登记的 profile/transcriptome/deconvolution，不接受客户端路径替换。返回工作表列表、指定工作表原始表头及最多五行预览；未指定多工作表时明确返回 requires_sheet_selection。
- 表头作为数据读取，保留重复列名和样本前导零；不存在的工作表和文本表格错误工作表参数返回中文错误。
- Docker 默认普通用户输入回归 12 项通过，覆盖双工作表、重复表头、编号保留和数据集隔离。
- 该接口仅结构查询，映射保存、准备派生输入、实际分析采用选择工作表、前端映射交互和 API 文档仍未完成。尚未部署，完整计划继续。


## 工作表与编号列映射保存、实际输入接入

- 新增项目资产 prepare-input / input-schema 接口；通过既有项目归属检查选择原始资产，映射明确指定工作表和样本/基因编号列。多工作表必须明确选择，不改写原文件；样本列标准化为 sample，表达基因列标准化为 Gene 并置首列。
- 整理 CSV 登记为独立 prepared_input 资产，原始资产元数据记录当前整理版本、选项和源/整理文件 size/mtime。项目输入解析使用当前整理文件；结构查询仍读取原文件，允许后续重新映射。编号字符串前导零保留，不自动改名样本值或改变统计单位。
- 来源追踪仍指向原始资产 ID，同时记录实际分析文件；工作进程检查源文件/整理文件变化及映射替换，旧上游结果也按当前映射复核。普通资产删除入口可管理已登记的整理资产。
- Docker 输入/来源/持久队列回归 31 项通过；改为实际项目 HTTP 保存与查询接口后输入回归 13 项通过，覆盖指定工作表、原文件字节不变、实际路径解析、来源 ID 和原文件变化拒绝。
- 未部署。前端映射交互、映射重置/版本历史、分组及配对配置、完整分析计算采用映射的 RQ 验收仍需补齐。整理阶段目前 pandas 完整读取选定表格，大矩阵流式整理仍需按实际规模优化；原质量检查的表达扫描仍按行。完整计划继续。


## 映射面板接入数据检查步骤

- 新增 InputMappingPanel，在正常检查与尚未识别输入的分支均可打开。限定当前项目/数据集原始表格，分页读取资产列表，选择工作表和编号列，展示原始前五行，保存后调用向导重新检查。
- 映射请求传递真实资产 ID、工作表和列名；处理中禁用操作，显示错误和保存结果。源工作表始终从原文件读取，已保存编号列可回显。
- 空首工作表返回工作表目录和空预览，允许继续选择其他工作表，不因首表为空封死映射入口。
- Docker 映射交互测试通过（数据集过滤→切换工作表→保留 0001→保存请求→重新检查回调），最终生产构建通过；后端输入/映射 13 项通过，相关 diff --check 通过。
- 尚未浏览器验收或部署；仍需映射修改后的配置失效处理、重置/历史、更多异常交互及真实映射→RQ 分析验收，完整计划其他分析/运维事项继续。


## 映射变更配置清理与实际分析验收

- 映射保存后向导清空旧检查、模块参数、已选任务结果及 URL 上游/任务引用；保留模块选择和独立工具固定预设，重新检查成功后要求重新确认分组/样本/指标。复查失败向映射面板传递错误，避免继续宣称复查成功。
- 新增 test_mapped_batch_rq.py：说明页+数据页工作簿，通过实际项目 prepare-input 接口选择实验数据页与自定义编号列，再提交真实 RQ 双 Profile 分析。断言任务实际 input_assets 为整理路径、source_assets 为原始资产 ID、原文件字节保持，并验证 CSV 分页和跨任务归档。
- Docker 默认普通用户真实映射 RQ 链 1 项通过；映射/向导相关前端 5 项通过（既有模块发现测试存在 act 提示，不影响通过）；最终生产构建及相关 diff --check 通过。
- 尚未发布；仍需浏览器完整交互、映射重置/历史、配对/分组更深配置、其余分析链及完整计划运维和最终部署事项。


## 恢复原始输入与映射失效检查

- 项目资产 prepare-input 增加 DELETE，取消当前 input_preparation，不删除原始或已登记的整理文件。界面保存后显示恢复原始文件操作，恢复后重新检查并清理旧分析配置。
- 工作进程来源复核改为始终比较当前有效输入路径，修复重置后因为没有活动映射而跳过旧整理路径检查的问题；旧排队任务明确要求重新提交。
- Docker 输入及真实映射 RQ 回归 15 项通过，验证原始/整理文件均保留且旧任务拒绝；前端保存→重置→再次检查交互通过，生产构建通过。
- 尚未部署。映射历史展示、浏览器验收及完整计划其余分析和运维工作继续。


## Linux 上传上限统一配置

- Compose 为 API/worker 与 Nginx 共用 UPLOAD_MAX_MB（正整数，默认 100）；Nginx 镜像通过启动模板生成配置。Flask 读取同一变量，不再固定上限。
- 两层超限响应均为中文 JSON，并说明整个请求的大小限制；部署说明补充表单开销、前置代理及重建容器要求。
- Docker 后端回归 2 项通过；Nginx 模板语法通过，隔离网络实际 1 MiB 上限的超限请求返回 413 和正确中文 JSON，临时验证容器已清理。
- 尚未重建发布运行镜像。此项不代表断点续传、大矩阵流式处理或完整 Linux 部署验收已经完成；完整计划继续。


## 文本矩阵分块整理

- CSV/TSV 整理改为逐批读取与写入，不再先创建整表 DataFrame；批次行数按列数调整，最多 5000 行，目标最多约 25 万单元格。字符串编号、文本数值及表达矩阵基因列置首规则保留。
- 编码回退覆盖迭代期间发生的解码错误，每次从空输出重新写入，避免文件后段出现非 UTF-8 内容时重复或遗漏前段数据。原始文件不变，失败仍通过原有事务清理新文件。
- Docker 分块/输入检查/真实映射 RQ 回归 17 项通过；新增后段中文编码回退后，分块专项 3 项通过，验证超过两批的数据完整性和前导零。
- 工作簿当前保留原读取方式；样本指标和浸润质量检查仍有整表读取，大矩阵整体内存优化未全部完成。尚未构建发布，完整计划继续。


## 工作簿分批整理

- xlsx/xlsm 使用 openpyxl 只读工作表迭代，按列数限制每批大小；明确选择工作表，保留字符串编号、日期、缓存公式值和内部空行，忽略数据之后仅格式化的空行。超出已识别表头的非空列明确报错，避免截断数据。旧 xls 保留原读取方式。
- 超过一万行、多工作表、中文、前导零、日期、布尔值及内部空行的整理输出与既有 pandas 读取后 CSV 输出逐字节一致；测试禁止新路径调用 pandas.read_excel。
- Docker 分批整理、输入检查及真实映射 RQ 分析共 19 项通过（9.42 秒）。原始工作簿字节不变。
- 尚未构建发布。只读工作簿仍需加载工作簿元信息/共享字符串，此证据不是任意规模恒定内存的承诺；旧 xls 和其他质量检查整表读取尚需处理，完整计划继续。


## 任务查询补偿队列终态

- 单任务查询对仍排队/运行且登记了 Redis 队列引用的任务，核对真实 RQ 状态及任务归属。明确 failed/stopped/canceled/finished 而业务记录未结束时，原子更新为 interrupted，并结束未完成子任务。已完成业务记录不改写。
- Redis 连接异常、缺失记录和仍在运行/排队的记录不推断失败；此项不提供算法断点续算。
- Docker 持久队列和真实映射批次回归 21 项通过；真实 Redis 合成记录 started→failed 后补偿、重复查询完成时间稳定专项 1 项通过。
- 尚未发布。当前为单任务查询触发，列表批量恢复、强制终止整个容器的恢复时机和重启演练仍待完成，完整计划继续。


## Linux 工作进程强制结束与维护恢复演练

- 工作进程队列维护间隔增加 ANALYSIS_QUEUE_MAINTENANCE_SECONDS，默认 60 秒；Compose 传递该配置，部署说明解释心跳、维护调度和无工作进程时的限制。
- Docker 内独立 Redis 测试队列实际运行 sleep 合成任务，以 SIGKILL 结束 RQ 工作进程及计算子进程组成的进程组，确认回调无法执行且平台暂时仍运行。等待真实心跳自然到期后，由替代 Worker.clean_registries 清理，真实 RQ 终态变为 failed，平台查询补偿父任务及未完成子任务为 interrupted。
- 队列恢复专项 2 项通过（66.52 秒），没有提前修改过期时间、没有生产任务参与；测试清理自身任务和队列。
- 此验证为 Linux 容器内进程组终止，不等同于 Docker Compose 服务强制终止、重启的完整部署验收；当前镜像尚未更新发布，列表批量恢复及完整计划其余内容继续。


## 输入映射浏览器验收与移动裁切修复

- Docker Chromium 在真实 /analysis/tools/profile 页面、合成 API 下验证文件选择、工作表切换、前导零预览、编号列选择、保存并复查、恢复原文件并复查；没有页面运行错误。
- 截图发现 390px 下向导网格按内容最小宽度撑开，导致面板和操作按钮被页面裁切。向导与映射网格设置 minmax(0,1fr)，阶段容器 minWidth=0；补充面板/保存按钮真实边界断言，不能仅依赖页面 scrollWidth。
- 修复后桌面与移动验收通过，并人工查看移动截图确认输入控件和操作按钮完整可见。Docker 前端生产构建通过，相关 diff --check 通过。截图保存在会话可视化目录 mapping-desktop.png / mapping-mobile.png。
- 浏览器接口为合成数据；真实工作簿→RQ 分析已有独立后端回归，不将二者表述为浏览器端到端真实后端验收。尚未发布，批次依赖浏览器验收和完整计划其余事项继续。


## 工作流候选镜像构建与集成验收

- 后端 immune-platform-api:workflow-candidate 构建完成，镜像 sha256:33eca5e30aff977d3256734e5dc4b7605815ff0794e9fad65cde821c260505ea，默认 UID/GID 10001；构建时 SoNNia 与 R/Bioconductor 完整检查通过。
- 前端 immune-platform-web:workflow-candidate 构建完成，镜像 sha256:0eb1e10aadaf844fb7fa5d844f666df3efb1cc81a1707f11edd90a8c9471856a。
- 集成验收暴露两项测试问题：旧 profile_app fixture 向 sys.modules 注入 UMAP=object，污染后续真实降维；前端 Stage2 测试替身直接传鼠标事件，被当作映射变更标志。移除全局占位对象，测试替身按实际组件无参调用检查。
- 修复后候选镜像内后端实现配合最新测试，输入/整理/上传/队列/真实映射链/真实 PEP 链/结果归档及分页 54 项通过（56.93 秒）；候选依赖下最新前端测试 31 文件、101 项通过。测试代码修正未改变候选镜像中的产品实现。
- npm audit --omit=dev 报告 react-router/react-router-dom 高危两项（RSC 模式 CSRF 公告），当前前端是 Nginx 静态 SPA；仍需核对并更新依赖，不能据此声称完整依赖审计通过。
- 当前业务任务只读统计没有排队或运行项，但更新前必须重新确认。运行服务仍为旧 root 镜像，尚未迁移数据卷权限或切换候选版本；备份、权限与实际服务更新继续，完整计划未完成。


## 2026-09-14 工作流版本已部署到当前 Docker 服务

- react-router-dom 更新为 7.18.2，同步容器生成的锁文件；生产依赖 npm audit --omit=dev 零报告项。开发依赖仍有 10 项报告，不表示完整依赖审计全部通过。更新后前端 101 项回归通过，生产镜像构建成功。
- 更新前 SQL 活动任务、Redis 排队和执行登记均为 0。停止六个服务后，将五个数据卷和原部署配置冷备份到 E:/Desktop/project/immune-platform-backups/pre-workflow-20260914.tar.gz；工具完整读取压缩包校验通过，大小 4,455,372,529 字节。备份含部署凭据，不提交仓库。
- 数据卷初始化处理 63,478 个目录/文件；随后以既有数据卷启动候选镜像。Compose 增加 WEB_IMAGE_TAG，当前 .env.docker 的前后端版本均选择 workflow-candidate。API/worker 镜像 sha256:33eca5e30aff977d3256734e5dc4b7605815ff0794e9fad65cde821c260505ea；web 镜像 sha256:6c0a291cde04f31728b4c588d08e36b224ff32252e5abcb760c3b43ede108332。
- 六个服务健康；API 实测 UID 10001、临时目录写入正常；工作进程心跳通过；真实 HTTP 健康、免登录分析入口、历史结果接口及一个历史文件读取返回 200。Docker Chromium 使用真实接口打开分析中心，无登录门禁和页面运行错误。
- 本次完成当前机器 Linux Docker 容器的版本更新，前述“尚未部署”记录是历史状态。未提供目标 Linux 服务器，不能声称目标服务器部署完成；完整计划中的其余分析链、项目分组配置、结果体验、列表状态恢复和应用级恢复演练继续。


## 功能富集复用差异表的服务能力

- GoKeggEnrichmentService 增加 deg_directory / differential_metadata 分支：保留表达矩阵原流程，显式选择复用时复制完整 DEG 表并直接送入既有 R 脚本，不重新计算差异或变更上游 significant 标记和统计量。
- 排除 DEG_significant 子集文件；检查目录范围、基因/筛选字段及 GSEA 所需 t 列。记录来源目录、文件及上游元数据；分析元数据现在在归档前写入，修复压缩包遗漏元数据。
- Docker 服务回归 2 项通过，验证禁止调用差异重算、复制文件字节相同、原文件不变、元数据归档和缺少完整表/排序列报错。此处 R 子进程为测试替身，不宣称真实富集计算验收完成。
- 尚未接入项目产物候选、请求来源绑定、前端选择及真实 DEG→富集 RQ 链，未更新运行镜像。完整计划继续。


## 项目差异结果候选与富集任务绑定

- 新增 GET /api/script-hub/go-kegg-enrichment/sources，按已拥有项目和数据集查询 expression 模式差异分析，显示来源完成状态、完整 DEG 文件和不可用原因。核对来源输入路径/版本、结果目录范围和文件存在性。
- 富集运行支持 input_mode=deg + upstream_artifact_id。后端从候选解析实际目录与来源元数据，忽略客户端自填目录；持久任务记录 upstream_input、逐文件输入描述和来源参数。工作进程再次检查来源状态与文件快照，结果改变拒绝旧任务。
- Docker 新来源绑定、富集复用及既有来源回归 8 项通过：真实 HTTP/数据库候选、跨数据集拒绝、客户端路径不能覆盖、来源元数据传递、排队后文件修改和来源失败拒绝。执行器和 R 子进程仍为专项测试替身，真实完整富集链尚未验收。
- 前端结果选择、批次绑定、接口类型文档与实际 DEG→富集 RQ 链继续；此次代码尚未更新运行镜像，完整计划未完成。


## 2026-09-16 富集前端来源选择与范围调整

- 按用户最新要求，本轮暂不接入 BCR 模块，其他完整计划要求继续；已同步计划首页范围。
- 富集配置增加表达矩阵/已有差异结果两种输入方式；复用时按项目数据集加载候选，显示比较和时间、不可用原因及上游阈值，不展示不会生效的差异重算参数。切换为表达矩阵清除产物引用。
- 复用模式暂停表达矩阵检查；提交前要求选择产物，请求不附带客户端差异目录或表达路径。表达检查中残留 comparisons 英文提示改为中文。
- Docker 前端交互测试通过，覆盖候选请求作用域、不可用项、选择标识、上游阈值展示和模式切换；生产构建和相关 diff --check 通过。
- 前后端改动尚未发布。浏览器与真实 R 富集 RQ 链、批次来源衔接、接口文档等仍需完成，完整计划继续。


## 2026-09-16 差异表达到富集的真实队列串联

- 富集 R 脚本收集 GO、KEGG 和 GSEA 主分析异常并使任务失败，避免吞掉分析异常后返回完成；正常无显著项不变。真实 R 服务运行生成 4 张表和 8 张图。
- 批次后端允许 expression 模式 volcano → go-kegg-enrichment，限定当前前序任务的差异产物，拒绝 V/J 使用差异作为基因富集来源。
- 独立临时 Redis、临时数据库和合成表达矩阵下，真实 RQ 工作进程执行差异及富集，核对父子任务完成、来源任务精确绑定、完整差异表字节一致、富集表生成及压缩包 HTTP 200。与产物/批次专项合计 16 项通过，63.55 秒；此链关闭 GSEA，不代表 GSEA 实际链已验收。测试 Redis 已停止并自动删除。
- 前端组合分析增加本批次差异表达来源；无论模块选择顺序如何，执行时前序差异排在富集之前，移除旧产物及表达路径，服务端绑定新结果。V/J 使用模式不显示该选项。既有克隆分析依赖继续保留。
- Docker 前端批次和富集来源 11 项交互测试通过，生产构建通过；OpenAPI 草案补充差异来源查询及批次衔接契约。
- 以上代码尚未发布到运行镜像；浏览器完整操作、GSEA 实际链、目标 Linux 主机与应用级恢复验收仍待完成。BCR 暂不接入，完整计划保持执行中。
